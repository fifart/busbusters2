  <strong>Company:</strong>
  <%= @posting.user.company %> / 
  <strong>Category:</strong>
  <%= @categories.find(@posting.category_id).title %> / 
  <strong>Posted on:</strong> <%= @posting.created_at.strftime("%d %B %Y, %l:%M%P") %><br>
  <strong>Title:</strong>
  <%= @posting.title %><br>
  <strong>Description:</strong><br>
  <%= @posting.body %>
  <hr/>
<% if user_signed_in? %>
<% if @posting.user_id == current_user.id %>
<%= link_to 'Edit', edit_posting_path(@posting) %> |
<% end %>
<% end %>
<%= link_to 'Back', root_path %>
